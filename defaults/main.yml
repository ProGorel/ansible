vars:
  my_root_path: /home
  my_compose_path: "{{ my_root_path }}/docker-compose.yml"
  my_data_path: "{{ my_root_path }}/data"  
  image_nginx: nginx
  ports_nginx: [80:80]
  image_redis: redis
  image_postgres: potgres
  volumes_postgres: [/tmp/pgdata:/var/lib/postgresql]
