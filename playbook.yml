---
- hosts: localhost
  connection: local
  become: yes
  vars_files:
    - var/default.yaml

  tasks:
  - name: docker-compose
    template:
      src: docker-compose.yml.j2
      dest: "{{ my_compose_path }}"
      mode: 0700
    
